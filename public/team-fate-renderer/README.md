# Team Fate PokeMMO Renderer Assets — Stage 5

Extracted from addons.pak.

- 5 skin bases
- 52 animation frames per skin
- 7 preview poses per skin
- 697 named cosmetics
- Cosmetic layers are normalized to 57x56 and magenta transparency keyed
- manifest.json contains layer/icon paths and slot metadata
- renderer.ts provides a browser Canvas renderer

Expected public path:
`/public/team-fate-renderer/`

Then import `manifest.json` and `renderer.ts`.
